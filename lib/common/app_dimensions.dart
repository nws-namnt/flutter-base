/// Shared layout dimension constants used across the app.
class AppDimensions {
  /// Base spacing unit, in logical pixels.
  static const double spacing = 4;

  /// Default padding value, in logical pixels.
  static const double padding = 16;
}